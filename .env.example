# Redis (for testing — docker-compose: port 6379)
REDIS_URL=redis://localhost:6379

# PostgreSQL (for testing — docker-compose: port 5433)
POSTGRES_URL=postgres://hitlimit:hitlimit@localhost:5433/hitlimit_test

# SQLite (for testing)
SQLITE_PATH=./test-ratelimit.db

# Docker usage:
#   docker compose up -d redis postgres    # start services
#   docker compose down                     # stop services
#   docker compose ps                       # check status
